'use strict'

// 设置矩阵的大小，x和y分别表示矩阵的宽和高。
var rectangle = {
	x: 10,
	y: 15
};

// 设置方块的大小
var blockSize = 40;

// 设置倒计时
var countDown = 300;

// 设置目标分数
var successfulScore = 100;

// 设置不安全的时间
var unsafeTime = 50;